#!/bin/bash

# Claude aliases - simple shortcuts for common operations

# Core commands
alias cl='claude'                # New session
alias clc='claude --continue'    # Continue last session
alias clp='claude --print'       # One-off command (non-interactive)